# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'graphql-live-notes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for graphql-live-notes
  pod "Apollo"
  pod "Apollo/SQLite"
  pod "Apollo/WebSocket"

  target 'graphql-live-notesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'graphql-live-notesUITests' do
    # Pods for testing
  end

end
